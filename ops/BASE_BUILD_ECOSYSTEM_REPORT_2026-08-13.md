# Base Dashboard / Ecosystem Report

Date: 2026-08-13. “Base Build” is now **Base Dashboard** at `dashboard.base.org`; the old Farcaster-mini-app-first model is superseded in the Base App by registered standard web apps plus wallet integrations.

## 1. Current state

Decay Labs is registered as Base Dashboard app `6a6018e4426d14cfbad57663` with primary domain `decaylabs.online`. Weekly leaderboard visibility and Base App notifications are enabled. Last-week dashboard metrics observed: 0 opens, 0 active users, 0 transactions, 0 gas and 0 entry-point actions. This is a real cold-start signal, not a tracking estimate.

The two completed Inbox tasks were:

- **Get started with Builder Code** — relevant and already implemented in code.
- **Submit Your Appcoin** — not recommended; Decay Labs has no appcoin strategy and adding one would distract from art/product demand.

## 2. Changes applied

- Filled and saved the empty 157/180-character Base app description.
- Verified app name, 30-character tagline and primary domain.
- Verified weekly leaderboard visibility.
- Verified Builder Code `bc_yb6cmebf`; the encoded ERC-8021 suffix in checkout code matches it.
- Built `public/base-app-thumbnail.jpg` at 1200×628 and 164 KB. Upload was blocked by Chrome extension file-URL permission, so the asset remains ready locally.
- Added a real standard-web discovery experience (`/find-your-subject`) and live analytics events.

## 3. Wrong, missing or stale links/proof

- Description was empty: fixed.
- App screenshots and thumbnail remain missing in Dashboard.
- Dashboard app metadata does not expose GitHub, Talent, contract or Farcaster proof in the inspected App Information surface.
- The website still carries Farcaster manifest support for Farcaster, but Base App’s current April 2026 model is standard web app + Base Dashboard metadata. Treat the manifest as Farcaster proof, not current Base discovery metadata.
- Account personal name/email are not provided; this is optional privacy, not a product failure.

## 4. Proof chain

Current verifiable chain:

`Builder Code bc_yb6cmebf → Decay Labs Base Dashboard app → decaylabs.online → Vercel production → Subject/Mini App web experience → ERC-721 contract → OpenSea/BaseScan → GitHub starter`

Missing public surfaces: Dashboard screenshots, thumbnail upload, a clear Dashboard-to-GitHub link surface, real attributed transactions, and external users.

## 5. Builder proof

Verified repository/live claims: Base ERC-721, IPFS media/metadata, 1,000 deterministic Subject records, signed Farcaster manifest, exact deep links, Subject share HTML, guarded Seaport fulfillment, ERC-8021 suffix, privacy-safe events, Vercel production and 50 tests. Do not claim Dashboard attribution performance yet: metrics are zero.

## 6. Mini App / standard app proof

For Farcaster, the signed manifest and compose flow remain useful. For Base App, current official guidance says registered apps are standard web apps after 9 April 2026. Decay Labs already loads as mobile web and uses viem/injected wallet interaction; the high-priority work is complete Dashboard metadata and real distribution, not more legacy Mini App code.

## 7. Current ecosystem opportunities

- **Base Dashboard/App discovery and weekly leaderboards:** active, fit high after metadata/screenshots are complete.
- **Builder Codes / ERC-8021 analytics and future rewards:** active, fit high; already encoded. No reward is guaranteed.
- **Base Batches 003:** cohort already selected; not active for new Decay application.
- **Base Ecosystem Fund:** active but 2026 focus is global onchain finance; Decay art product is a weak fit. Do not contort the project.
- **Base events:** current official events surface exists; evaluate individual future calls when announced.
- **Free contract monitoring:** useful security opportunity if current official application remains open; requires account/application action.

## 8. Cross-platform loops

- Base Dashboard → live app → match → exact Subject share → Farcaster/Base social discovery.
- Farcaster → exact Subject → technical proof → Base Dashboard/GitHub credibility.
- Talent aggregates builder identity; Base Dashboard owns Base app/attribution proof. Do not duplicate the same bio.
- GitHub proves implementation; Base Dashboard metrics prove it reaches live users. Currently implementation exists, usage does not.

## 9. Reusable builder asset

`base-miniapp-buy-starter` can be strategic if released as one stable, audited version with current Base standard-app language, Builder Code attribution, threat model and live demo. It should solve another builder’s problem; stars/commits must not be farmed.

## 10. More Base-native without feature bloat

Keep Base’s role to ownership, provenance, transaction settlement and attribution. The matching experience does not need a transaction. The collect action does. Builder Code makes real transactions attributable without changing the ERC-721. Do not add tokens, points, sponsored transactions or another contract merely to appear Base-native.

## 11. Scorecard

| Area | Current | After | Ideal | Gap / action |
|---|---:|---:|---:|---|
| Profile/app metadata | 4 | 7 | 10 | Upload thumbnail + 3 screenshots |
| Project presentation | 5 | 7 | 10 | Show product loop visually |
| Builder identity | 6 | 7 | 9 | Public human/GitHub linkage |
| Technical proof | 8 | 9 | 10 | Intentional public release |
| Onchain proof | 8 | 8 | 10 | Real attributed users/transactions |
| GitHub linkage | 5 | 6 | 9 | Dashboard surface limited; use site proof |
| Farcaster linkage | 7 | 8 | 9 | Real share/use evidence |
| Talent linkage | 4 | 5 | 8 | Resolve Talent domain/project mapping |
| App proof | 6 | 8 | 10 | Screenshots, Base App opens |
| Discoverability | 2 | 5 | 9 | Complete visuals + distribution |
| Ecosystem participation | 2 | 4 | 8 | Meaningful builder participation |
| Overall Base presence | 5.3 | 7.1 | 9.3 | Usage and account-level proof remain |

## 12. Manual actions

1. Enable Chrome extension “Allow access to file URLs,” then upload `public/base-app-thumbnail.jpg` and three 1284×2778 product screenshots.
2. Replace/rotate the invalid OpenSea key in Vercel and mark it Sensitive; no key value is recorded here.
3. Generate no Appcoin and submit no reward/grant transaction without a separate product case and explicit approval.
4. After a real checkout, verify ERC-8021 attribution in Base Dashboard/block explorer.

## 13. Only three Base jobs

1. Complete screenshots/thumbnail so the registered app can convert discovery.
2. Restore the OpenSea credential and verify one non-financial quote; later verify Builder Code on a real user-approved transaction.
3. Publish the matching/share loop through meaningful Base/Farcaster participation and measure Dashboard opens.

If only one: **complete the Base App product listing with strong visuals and send real users into the live matching loop**. Attribution without users remains zero.

## Final question

Did this optimize another profile, or connect product, builder identity and Base distribution? **It connects the system:** live app metadata, Builder Code, Vercel product, matching/share loop, contract and GitHub proof now form one chain. The remaining failure is not another missing feature; it is zero real distribution and incomplete Base visual merchandising.

